# v1.1 evidence — captured from the preview harness at side-panel width (420 px)

Generated by `node scripts/evidence.mjs` (headless Brave over DevTools, same click/type flow a user performs;
the AI call in shot 11 is stubbed with a canned `report_matches` because the harness has no network).

| # | Concern (from the Reddit comments) | Shot | What it shows |
|---|---|---|---|
| 1 | "show the exact titles and URLs that are about to leave the browser" | 04, 05 | "Before sending" lists every open tab and every library bookmark as it will be sent, names the provider host, counts what was kept back |
| 2 | "a tab can leak a customer name or a nasty query string" | 05 | `Statement for [email] – card [number]` and `…/statements/~` — email, card number, UUID and `?session=` never leave |
| 3 | "let people exclude domains" | 02, 03, 07 | Options list; 🔒 on the excluded tab at home; one-click 🔒 in the preview confirms with a toast |
| 4 | skip a single item for this conversation | 06 | ✕ on a row: 31 → 30 open tabs, "3 kept back" |
| 5 | private / intranet pages must never go to a vendor | 04 | `http://intranet/…` is absent; counted under "kept back" |
| 6 | "finding the right one three weeks later when you don't remember its title" | 09, 10, 11 | Typing a sentence → hint to press Enter; Enter → the same preview ("This search will send…"); result narrows the lists with a reason per hit |
| 7 | "showing why something was grouped" | 12 | One-line note under the proposed folder in Review |
| 8 | search badges show matches, not folder size | 08 | `figma` → Work 1 / Client A 1 instead of 2 / 1 |

Automated coverage: `npm test` (9 unit tests on the redaction rules) and `tests/harness` (30 browser tests, open `/test.html`).
