{
  "name": "tab-librarian",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "description": "Tab Librarian - AI librarian for your tabs (Chrome/Brave/Firefox, BYOK)",
  "scripts": {
    "dev": "vite build --watch",
    "build": "npm run check && vite build",
    "test": "node --test \"tests/unit/*.test.ts\"",
    "preview:setup": "node tests/harness/make-preview.mjs",
    "preview": "vite preview --port 4173 --strictPort",
    "test:ui": "npm run build && npm run preview:setup && npm run preview",
    "build:firefox": "npm run build && node scripts/make-firefox.mjs",
    "package": "npm run build && powershell -NoProfile -Command \"Compress-Archive -Path dist/* -DestinationPath tab-librarian.zip -Force\"",
    "check": "tsc --noEmit && node scripts/check-layers.mjs && npm test"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.123.0"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.8",
    "typescript": "^7.0.2",
    "vite": "^8.2.2"
  }
}
